export const config = {
  cosca: {
    // this comment must be preserved
    stringKey: 'value',
    numberKey: 1,
    boolKey: true,
    arrayKey: [
      'value',
    ],
    objectKey: {
      // this comment must be preserved
      nestedKey: 'value',
    },
  },
}
